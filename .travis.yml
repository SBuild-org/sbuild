language: java

jdk:
  - oraclejdk8
  - oraclejdk7
  - openjdk7

env:
  matrix:
    - SBUILD_VERSION=0.7.1
    - SBUILD_VERSION=0.7.5

before_script: "./sbuild-wrapper --version"

script: "./sbuild-twice.sh --clean clean all"
